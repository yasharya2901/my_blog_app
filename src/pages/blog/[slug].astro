---
import App from "../../layout/app.astro";
import BlogContent from "../../components/BlogContent/BlogContent";

const { slug } = Astro.params;
if (!slug) {
    return Astro.redirect("/");
}
---

<App title="Blog Post">
    <BlogContent slug={slug} client:load />
</App>